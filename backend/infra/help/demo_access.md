### 🎯 **Цель**

Добавить поддержку "демо-админов", у которых будет **отдельная, персональная база знаний** и **свои настройки ассистента**, не влияющие на основную (общую) систему.

---

### 🧱 **Текущая архитектура**

* Сейчас в проекте используется **единая база знаний**, общая для всех пользователей с админскими ролями.
* Эта база используется:

  * для отображения и редактирования в админке;
  * для работы чата с ИИ (например, генерации ответов на основе этой базы).
* Также имеются **общие настройки ассистента** (например, имя, промпт и прочее).

---

### 🔧 **Что нужно изменить**

1. **Добавить новую роль**: `demo_admin`.
2. При обращении к базе знаний или настройкам:

   * если пользователь — `demo_admin`, использовать **персональную базу** и **свои настройки** (создавать, если ещё нет);
   * если любая другая админская роль — использовать **основную (main) базу и настройки**.
3. После получения нужной базы и настроек — **передавать их во все функции** проекта, чтобы остальная логика не требовала изменений (реиспользовать существующие интерфейсы).

---

### 💡 Ключевая идея

Централизованно определять нужную базу знаний и настройки по `user_id` из токена и его роли, а затем "прокидывать" их в остальную систему без модификации бизнес-логики.
